# HMS AI Cockpit — v25.25 continuation

## Delivered tranche
`ACCOUNT_POOL_INTELLIGENCE_AND_ANTIGRAVITY_CONTROL`

- Codex pool ranking/roles/usage signals are visible in native GUI.
- Account policy metadata is editable without mutating/deleting OAuth tokens.
- Antigravity 2.0 is now a first-class native GUI page.
- Seamless vs restart fallback semantics are explicit.
- Antigravity Auto Switch participates in native GUI maintenance when enabled.

## Next recommended tranche
`USAGE_LEDGER_AND_RELEASE_MANAGER`

1. Durable deduplicated request ledger (beyond rolling log-window signals).
2. Per-model/account success/failure/latency dashboard.
3. Package updater + rollback UI with signed/hash-verified releases.
4. Safer one-click diagnostics bundle from GUI.
5. Adaptive pool policy that can recommend, then optionally apply, router strategy changes.

## Runtime test policy
Real runtime tests remain deferred by operator direction; defects found during actual use become follow-up remediation revisions.
