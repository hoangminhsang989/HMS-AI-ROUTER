# HMS-AI-ROUTER — v25.75 P0 Evidence Adversarial Fixtures Candidate

Date: 2026-08-24
Status: CANDIDATE — Windows CI execution unavailable

## Purpose

Increase deterministic negative-fixture coverage around the canonical seven-case external Windows/current-Codex evidence ingest/export path without changing production verifier semantics and without substituting synthetic results for real target evidence.

## Candidate scope

`_runtime/HMS_Codex_ExternalWindowsEvidenceAdversarialFixtures.py` adds proof-only coverage for:

- exact 7-case positive control;
- 6-case and 8-case matrix rejection;
- missing/unexpected/duplicate case IDs;
- case status `FAIL`;
- invalid and duplicate case report digests;
- non-dict case shape;
- nonce, run ID, report ID and packet-digest replay;
- invalid/short nonce, run ID and report ID;
- stale, future and invalid capture timestamps;
- wrong target OS, `codex_target=false`, wrong source classification, `local_only=true`, synthetic source flag;
- package and release-manifest digest mismatches, including invalid expected digests;
- Cockpit baseline drift;
- exporter rejection for wrong product/suite/verdict/certification, stage pass-count mismatch, failed stage, missing stage and unexpected stage;
- explicit preservation of all non-production authority flags.

`HMS_Codex_ExternalWindowsReviewPacketIngest.py` is changed only inside `synthetic_proof()` to invoke this child fixture suite. Runtime `verify_packet()` semantics are unchanged.

## CI observation

PR #5 head initially triggered Windows Promotion Safety run #71 (`32737605199`). The job (`97464086510`) completed as failure before any workflow step was materialized: `steps=null`, with no Checkout/Setup/Test execution available to inspect.

This matches the independent runner-start behavior already observed on PR #4 runs #69/#70. Therefore the current candidate is not classified PASS or code-failure from CI. Exact repository/account runner-start cause remains unproven with the available connector.

## Evidence boundary

Unchanged:

- Feature evidence score: `93.0%`.
- Production evidence score: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.
- No automatic production score mutation.

Do not merge this candidate solely because the negative-fixture design appears correct; require an executed proof result when CI/runtime execution is available.
