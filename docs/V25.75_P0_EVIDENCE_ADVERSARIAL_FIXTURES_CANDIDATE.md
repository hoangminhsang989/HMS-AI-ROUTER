# HMS-AI-ROUTER — v25.75 P0 Evidence Provenance Hardening Candidate

Date: 2026-08-24
Status: CANDIDATE — Windows CI runner-start unavailable; no executed proof graph yet

## Purpose

Harden the canonical seven-case external Windows/current-Codex evidence chain against contradictory certification metadata, freshness laundering and provenance substitution, while adding deterministic negative fixtures. This candidate is fail-closed and does not substitute synthetic proof for real target evidence.

## Runtime hardening

### Target certification exporter

`_runtime/HMS_Codex_ExternalWindowsCaseReportExporter.py` now:

- validates the exact canonical seven-case stage set;
- requires `stages_pass == 7` and `stages_total == 7`;
- requires `summary.production_certified == true`;
- requires the official production verdict and certification class;
- rejects missing/invalid source `generated_utc` instead of replacing it with packaging time;
- hashes the exact selected target-certification bytes;
- preserves the original source capture time in all seven exported case reports;
- exports the exact source report SHA-256 without granting production authority.

### Evidence packet runner

`_runtime/HMS_Codex_ExternalWindowsEvidenceRunner.py` now requires the original `--source-certification-report` in addition to the seven case reports. It:

- validates the selected source report against the same exact 7/7 production contract;
- records its exact byte SHA-256 and original capture time during preflight;
- re-reads/revalidates the source after preflight and fails if the SHA/capture changed;
- validates product/suite/status/target/provenance fields on each case report;
- requires all seven case reports to bind to the exact selected source SHA and capture time;
- uses the original source capture time as packet `capture_utc` rather than packaging time;
- signs `source_certification_report_sha256` and each case's source-report binding into the packet.

### Packet ingest

`_runtime/HMS_Codex_ExternalWindowsReviewPacketIngest.py` now fail-closes unless:

- the signed packet contains a valid source certification SHA-256;
- every valid case result carries a valid source-report SHA-256;
- all seven case source hashes are complete and identical;
- the case source hash equals the packet-level signed source certification hash.

This is a deliberate runtime verifier hardening change. Older statements that production verifier semantics were unchanged are superseded by this checkpoint.

## Proof-only coverage

`_runtime/HMS_Codex_ExternalWindowsEvidenceAdversarialFixtures.py` covers matrix, status, replay, timestamp, digest, source-classification, baseline and exporter negative cases.

`_runtime/HMS_Codex_ExternalWindowsSourceBindingProof.py` additionally proves in-memory/temp-file controls for:

- exact source-byte digest binding;
- exporter-to-runner preservation of source SHA/capture;
- rejection of missing/non-production source reports;
- post-export source mutation changing the digest;
- old case reports no longer matching a mutated source;
- packet-builder source revalidation and signed source binding guards.

Both child suites are invoked only from ingest `synthetic_proof()` and retain all non-production boundary flags.

## CI observation

GitHub-hosted Windows Promotion Safety has repeatedly failed before any workflow step was materialized, including:

- run #71 — `32737605199`;
- run #73 — `32737849684`;
- run #77 — `32738442292`;
- run #82 — `32739886073`.

Observed jobs report failure with `steps=null`; Checkout/Setup/Test did not execute. The exact repository/account runner-start cause remains unproven with the available connector. This is not classified as a code/test failure and is not classified PASS.

## Evidence boundary

Unchanged:

- Feature evidence score: `93.0%`.
- Production evidence score: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.
- No automatic production score mutation.

## Merge gate

Do not merge solely from static review. Require an executed Windows proof graph for the final candidate head. Real target-machine seven-case evidence remains a separate later gate and is not replaced by this candidate.
