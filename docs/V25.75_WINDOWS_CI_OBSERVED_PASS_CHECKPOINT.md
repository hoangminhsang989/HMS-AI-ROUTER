# HMS-AI-ROUTER — v25.75 Observed Windows CI Proof Graph Checkpoint

Date: 2026-08-24

## Verdict

`PASS_WINDOWS_GITHUB_HOSTED_PROOF_GRAPH_V25_75`

This checkpoint records an **observed GitHub-hosted Windows proof run**. It is CI/source/synthetic verification only. It is not target-machine UAC validation and it is not Windows/current-Codex production certification.

## Observed run

- Workflow: `v25.75 Windows Promotion Safety`
- PR observation run number: `63`
- Run ID: `32689734884`
- Job ID: `97321160108`
- Head commit: `d51e2f724cfae69d5ce1a1c14c3573b427bc0b23`
- Final workflow conclusion: `success`
- Runner OS: Microsoft Windows Server 2025, build `10.0.26100`, Datacenter
- Runner image: `windows-2025-vs2026`
- Runner image version: `20260818.207.1`
- Python: `3.12.10`

All configured proof steps through `Promotion-disabled safe fallback proof` completed successfully on the same run.

## Remediation discovered by the observed Windows gate

The PR walking-gate exposed and corrected proof/CI wiring defects without weakening runtime safety:

1. Certificate signer source proof was scoped to implementation so its forbidden double-dash literals could not self-match.
2. Reviewer CurrentUser-DPAPI integrity proof was scoped to implementation so the forbidden machine-scope literal could not self-match.
3. UAC runtime-validation proof was split into implementation slices, preventing its own forbidden elevation and already-closed literals from self-matching.
4. Recovery GUI authority proof became polarity-aware: it forbids certification/score-authority `True` while requiring the explicit `False` boundary.
5. Promotion-disabled safe fallback proof and launcher-chain proof now inspect only safe-fallback implementation when asserting no promotion controller.
6. Review-workbench CI invocation was corrected from positional `proof` to supported `--proof`.
7. Guarded GUI publication proof now verifies the actual nested-worker click-time binding:
   `ctl.record_review_action(... live_baseline_provider=provider.get_live_baseline ...)`.

No remediation granted automatic certification, production-score authority, arbitrary elevation, or raw-evidence mutation.

## What this PASS proves

The configured Windows proof graph parses/compiles and its source/synthetic security contracts pass on the GitHub-hosted Windows environment above. This includes the canonical seven-case contract shape, certificate/trust contracts, reviewer sealing/import contracts, recovery/UAC source contracts, bounded UAC validation harness proof, launcher/fallback chain, live-baseline/reviewer policy, decision ledger, workbench/controller/E2E fixtures and GUI wrappers.

## What this PASS does NOT prove

- It does not execute the harness `preflight` or `interactive` modes.
- It does not display or accept/cancel a real UAC prompt on the authorized target workstation.
- It does not close a real target Codex/ChatGPT process.
- It does not execute the canonical seven cases against the authorized real Windows/current-Codex target.
- It does not create or import a real production evidence packet.
- It does not certify Windows runtime.
- It does not promote the production evidence score.

## Frozen evidence boundary

Unchanged:

- Feature evidence: `93.0%`.
- Production evidence: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.
- Automatic production certification: false.
- Automatic production-score mutation: false.

## Next operational gate

1. On the authorized Windows target, run the UAC validation harness `preflight` read-only mode.
2. Validate one real UAC Cancel plus same-token replay rejection.
3. Validate one accepted UAC close of supported Codex/ChatGPT PID(s) plus same-token replay rejection.
4. Confirm restart-disabled/not-requested policies and unrelated processes remain non-elevatable.
5. Keep these recovery reports outside the canonical production evidence packet.
6. Then execute the real Windows/current-Codex canonical seven-case evidence flow, certificate signing, sealed reviewer import, dual human review and live baseline reconciliation before any human-authorized production-evidence promotion proposal.
