# HMS-AI-ROUTER — v25.75 Integrated Windows Safety Candidate

Date: 2026-08-25
Status: CANDIDATE — combined source authority prepared; full materialized Windows proof graph required before merge

## Scope

This integration candidate combines the two unmerged v25.75 safety candidates without moving `main`:

- Evidence / reviewer / release-identity authority candidate: PR #5 head `74cf28eeb23fca71ebc0abc6b9f23f743281ac86`.
- Windows recovery / elevation authority candidate: PR #4 head `fbe996de66dfcec4dde75c4d8de448b22572509f`.

The integration merge commit is `453459b2135b51f1007df0966427e7dd152ee551` with exactly those two parents.
Its merge tree is `804c1c42cc2c18f19a718d444f3dd0e39eef22f3`.

## Merge construction

The integration tree uses the final PR #5 tree as the base and overlays the exact committed PR #4 blobs for the eight non-workflow files:

- `_runtime/HMS_Codex_WindowsElevationCallerAudit.py`
- `_runtime/HMS_Codex_WindowsOneShotElevation.py`
- `_runtime/HMS_Codex_WindowsRecoveryAdversarialSimulator.py`
- `_runtime/HMS_Codex_WindowsUACRecoveryValidation.py`
- `_runtime/HMS_Codex_WindowsUACValidationBundle.py`
- `_runtime/HMS_GUI_RECOVERY_ENTRY.pyw`
- `_runtime/HMS_Run_UAC_Validation.ps1`
- `docs/V25.75_P1_WINDOWS_RECOVERY_ADVERSARIAL_SIMULATOR_CANDIDATE.md`

PR #4 and PR #5 overlap only in `.github/workflows/v2575-promotion-safety-windows.yml`.
That workflow was manually unioned. The union adds the PR #4 recovery caller-audit/adversarial-simulator path triggers, compile targets and proof steps to the complete PR #5 evidence/reviewer/release-authority graph. No proof gate from either candidate is intentionally removed.

Workflow union blob: `56f37d7fa59852f84c1fd5808c160f12bff8d120`.

## Combined safety authority

The combined candidate therefore contains, in one graph:

- exact bounded one-shot Windows elevation authority;
- caller-identity audit and deterministic recovery adversarial simulator;
- bounded UAC validation, session binding and Windows PowerShell operator proof;
- v25.75 artifact-bound target-machine certification wrapper;
- exact seven-case source/package/manifest provenance;
- certificate signer and independent signer trust authority;
- independent reviewer release identity authority;
- controller-level dual-authority ingest gate;
- replay/local-integrity/concurrency protection;
- click-time Cockpit baseline recheck;
- package-version-bound append-only decision ledger;
- reviewer policy/workbench/GUI release-identity gates;
- E2E negative fixtures and safe-fallback proofs.

## CI authority

Previous individual PR runs repeatedly failed before Checkout with `steps = null`, including PR #5 final-head run #131 (`32791607878`, job `97634107272`). Those results are infrastructure/runner-start blockers, not code PASS and not code/test FAIL.

This integration candidate must receive a fresh Windows Promotion Safety run on its final head. Merge is forbidden unless Checkout/Setup/Test actually materialize and the complete combined graph passes.

## Production boundary

Unchanged:

- Feature evidence score: `93.0%`.
- Production evidence score: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.
- No automatic production certification.
- No automatic production score mutation.

## Main authority

`main` remains unchanged at `02597822a2d3d0292a874b1ea67c3980a5abd953` while this candidate is under review.

## Next gate

1. Open one integration PR from `v25.75-integrated-windows-safety` to `main`.
2. Require a fresh Windows workflow run on the exact final integration head.
3. If steps materialize, inspect the complete graph and remediate only real test failures.
4. If `steps = null`, keep the integration PR unmerged and classify it as infrastructure blocked.
5. Even after source/CI PASS, real Windows/current-Codex target execution and human production promotion remain separate gates.
