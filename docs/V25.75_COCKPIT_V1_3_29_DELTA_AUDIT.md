# HMS-AI-ROUTER — Cockpit Tools v1.3.29 Delta Audit

Date: 2026-08-25  
Product scope: Codex-only  
Status: OPEN PARITY DRIFT — P0 credential safety source-closed/proof-wired; launch and session/provider migration source-reconciled/proof-wired; device-auth adoption decision OPEN; CI execution and baseline adoption still open.

> This audit records source/proof state. Documentation is not executable evidence and cannot close HMS runtime certification, Windows certification, baseline adoption, or production promotion.

## Frozen authority and reconciliation target

Frozen HMS evidence epoch:

- Cockpit Tools v1.3.28;
- release commit `82576b9634bad0a365abc51eba8f022fb0a50d97`.

Current upstream release under reconciliation:

- Cockpit Tools v1.3.29;
- release commit `83ce2d192cc954cc910ce89edf2d1f710c218798`;
- release commit time 2026-08-24T16:31:13Z.

HMS does **not** silently replace the frozen `COCKPIT_BASELINE = 1.3.28` with `1.3.29`. v1.3.28 remains the evidence authority for the existing sealed epoch only. v1.3.29 remains a reconciliation target until an explicit baseline-adoption decision is made after all required gates.

Frozen production authority remains unchanged:

- Feature `93.0%`;
- Production `55.2%`;
- `windows_runtime_certified=false`;
- `external_windows_target_evidence_imported=false`;
- `production_score_promotion_eligible=false`;
- no automatic certification or score mutation.

## P0 credential-safety reconciliation

Pinned exact-source authority used by `_runtime/HMS_Codex_CockpitV1329P0ParityProof.py`:

| Release | Path | Git blob SHA-1 |
| --- | --- | --- |
| v1.3.28 | `src-tauri/src/modules/codex_account.rs` | `842244a3b6948438a5b7a0df55655f7a366ff540` |
| v1.3.28 | `src-tauri/src/modules/codex_quota.rs` | `0cb8c5df897505fa572e363ddc67f49fea907bbf` |
| v1.3.29 | `src-tauri/src/modules/codex_account.rs` | `1e879fe36b9be9d3253a98d8eb30722415edc594` |
| v1.3.29 | `src-tauri/src/modules/codex_quota.rs` | `77ce9612cacfc70cced28feb84d510aa299dabcc` |

### P0-1 quota / refresh-token ownership

Exact v1.3.29 source chain:

`refresh_all_quotas_for_background → refresh_all_quotas_with_options(true) → refresh_account_quota → refresh_account_quota_once → codex_account::prepare_account_for_quota_query`.

Source invariants include:

1. background refresh excludes running official OAuth account IDs;
2. a valid access token short-circuits before the quota-query refresh-token lock;
3. official-runtime authority is synchronized after lock/reload when applicable;
4. access-token expiry is re-checked after synchronization;
5. a newly synchronized valid access token is reused rather than rotating the refresh token again;
6. managed quota refresh need is access-token based rather than ID-token keepalive based.

HMS classification: **SOURCE-CLOSED / PROOF-WIRED / CI EXECUTION PENDING**.

The canonical source-binding harness requires an executable fail-if-refresh-helper-called trap for valid access plus an expired-access positive control. Until a repository proof run actually executes, this is not HMS-side executable closure.

### P0-2 combined OAuth/API-Key credential owner

v1.3.29 records credential authority independently from runtime/provider authority through `credential_account_id`, `credential_email`, and `credential_token_generation`.

The combined writer keeps OAuth auth material owned by the OAuth account while the API-Key runtime account keeps provider/config ownership. Bound OAuth refresh resolves the bound OAuth owner, generation checks prefer newer authority, and exact upstream tests cover owner projection, rotation adoption, rotation without `last_refresh`, owner persistence, legacy projection upgrade, and newer-generation reuse.

HMS classification: **SOURCE-CLOSED / PROOF-WIRED / CI EXECUTION PENDING**.

The canonical source-binding proof requires seven owner/generation assertions. Source evidence cannot substitute for executable HMS proof or Windows runtime evidence.

## OAuth runtime correction and device-code delta

Connector scopes and the 10-minute ID-token lead are **not** a newly introduced Tauri-runtime semantic between v1.3.28 and v1.3.29. Both Tauri releases already use:

- `openid profile email offline_access api.connectors.read api.connectors.invoke`;
- `ID_TOKEN_REFRESH_LEAD_SECONDS = 10 * 60`.

The `cockpit-core` copy synchronizes from older base scopes / 15-minute lead in v1.3.28 to the Tauri values in v1.3.29. This is a real core-library source synchronization delta, not a new canonical Tauri behavior.

Device-code OAuth is the material new runtime capability in v1.3.29. `_runtime/HMS_Codex_CockpitV1329DeviceAuthDeltaProof.py` source-characterizes endpoint, deadline, polling, single-active-flow authority, restart non-revival, common code-exchange path, and login-ID-scoped cancellation behavior.

Classification: **SOURCE-CHARACTERIZED / PROOF-WIRED / ADOPTION DECISION OPEN**.

### Device-auth decision authority

The adoption decision is now stored separately from validator logic:

- `_runtime/HMS_Codex_CockpitV1329DeviceAuthAdoptionDecision.json`;
- current Git blob: `32ec231d1a9b1d20b2d6ae4555fcf64795bb20a5`;
- current decision: `OPEN`;
- `accepted_by=""`;
- `rationale=""`;
- `risk_acknowledged=false`;
- `parity_proof_ref=""`.

`_runtime/HMS_Codex_CockpitV1329DeviceAuthAdoptionContract.py` reads the decision file fail-closed and SHA-256-binds the loaded bytes. Missing/unreadable/invalid JSON or a record bound to the wrong product/release/commit becomes `DEVICE_AUTH_DECISION_RECORD_INVALID`.

Decision semantics remain:

- `OPEN` must not carry acceptance authority;
- `ACCEPT_CAPABILITY_GAP` requires an explicit acceptor, rationale, and risk acknowledgement and must not claim a parity proof;
- `REQUIRE_PARITY` remains unresolved until implementation proof is ready and an explicit proof reference exists;
- neither decision record nor validator may independently authorize baseline adoption, Windows certification, or production-score mutation.

The decision authority file is included in the canonical Windows workflow path triggers so any future device-auth decision edit must re-run the Windows graph.

## Launch confirmation-before-mutation

`_runtime/HMS_Codex_CockpitV1329LaunchConfirmationProof.py` source-reconciles the launch-state mutation boundary against exact v1.3.28/v1.3.29 upstream blobs.

Covered entrypoints:

1. account switch/start opens the preview before account switch/start mutation;
2. local API Service opens the preview before selected-instance binding/service activation;
3. instance start awaits `onBeforeStart`, cancel resolves false, and `startInstance` occurs only after a confirmed true result.

Boundary definition: **launch/client-activation mutation only**. Explicit configuration/tool actions inside the preview are not misclassified as launch mutation.

Classification: **SOURCE-RECONCILED / PROOF-WIRED / CI EXECUTION PENDING**.

This proof remains source-only and reports `windows_runtime_certified=false`, `production_score_promotion_eligible=false`, and `baseline_adoption_authorized=false`.

## Session/provider migration safety

`_runtime/HMS_Codex_CockpitV1329SessionProviderMigrationProof.py` pins exact v1.3.28/v1.3.29 blobs for:

- `src-tauri/src/modules/codex_session_visibility.rs`;
- `src/components/codex/CodexSessionVisibilityRepairModal.tsx`;
- `src/services/codexInstanceService.ts`.

Required source invariants are:

1. **rollback on failed mutation** — backup occurs before write and a failed repair restores the backup fail-closed;
2. **selected scope only** — target provider, selected session IDs, and selected instance IDs are preserved through frontend → service → Tauri/runtime selection;
3. **stopped-target requirement** — deep provider migration sets `require_stopped_instances=true` and rejects a running target before mutation unless the operation is dry-run;
4. **provider-bound filtering/discovery** — provider candidates are observed from configuration and official state/SQLite authority; already-matching provider records are not rewritten.

The proof also checks upstream instance-scope regression coverage and contains adversarial transition checks for:

- running target → no backup/no mutation;
- unselected target → no mutation;
- failed write → backup + rollback;
- selected/stopped positive control → apply.

Relevant PR #7 commits:

- `ea3a6dc65f1102df4461403a9c4de6a8d1b0a7bb` — add migration proof;
- `aafc005f94383cd4320cd9d6f6fced9ea4aa09c9` — bind baseline reconciliation to migration proof;
- `24206bd78e93713fc0165851b68e7b8d655e73d3` — harden provider-discovery/upstream-test anchors;
- `a8b45c9b316232b5e97bbba3f238abe63ab2823f` — execute in source-only lane and update baseline assertions;
- `e652a213909e4bfd4d941cb755bf8bc52a220077` — gate the Windows graph on the migration proof.

Classification: **SOURCE-RECONCILED / PROOF-WIRED / CI EXECUTION PENDING**.

No real upstream Rust runtime or Windows target was executed by the blocked GitHub jobs.

## Baseline reconciliation contract

`_runtime/HMS_Codex_CockpitBaselineReconciliationContract.py` derives launch and migration states from their exact-source proof verdicts rather than from manually flipped strings.

When both source proofs pass their exact commit/authority checks, current states are:

- `quota_refresh_token_ownership=SOURCE_CLOSED_PROOF_WIRED`;
- `combined_oauth_api_key_credential_ownership=SOURCE_CLOSED_PROOF_WIRED`;
- `launch_confirmation_before_mutation=SOURCE_RECONCILED_PROOF_WIRED`;
- `session_provider_migration_safety=SOURCE_RECONCILED_PROOF_WIRED`;
- `device_auth=SOURCE_CHARACTERIZED_PROOF_WIRED_DECISION_OPEN`.

Therefore the only current logical adoption blocker is:

`device_auth:SOURCE_CHARACTERIZED_PROOF_WIRED_DECISION_OPEN`

This does **not** adopt v1.3.29. `TARGET_BASELINE_ADOPTION_AUTHORIZED=false` remains a separate authority and the frozen baseline remains v1.3.28.

## CI execution state

Both GitHub-hosted runner labels remain blocked before runner allocation.

Exact head `e652a213909e4bfd4d941cb755bf8bc52a220077`:

- P0 Source Proof (Non-Windows) #14 — run `32821022688`, initial job `97718956348`, `steps=null`;
- targeted rerun attempt — job `97719302679`, again `steps=null`; log download returned `404 BlobNotFound` because no runner log blob was created;
- Windows Promotion Safety #178 — run `32821022602`, job `97718956144`, `steps=null`.

Exact head `cb5ee20b0ee5ae10a8899b220f84f699fa1f5f51` after externalizing the device-auth authority and adding the authority-file trigger:

- P0 Source Proof (Non-Windows) #17 — run `32821352191`, job `97719958604`, `steps=null`;
- Windows Promotion Safety #181 — run `32821352131`, job `97719960056`, `steps=null`.

Correct classification: **pre-runner repository/account infrastructure failure**. These results are neither proof PASS nor code/test FAIL. Checkout, Python setup, compile, P0 proof, launch proof, migration proof, baseline contract, Windows graph, and evidence execution did not occur.

No stronger cause such as billing, policy, or runner capacity is claimed because the available repository connector does not expose evidence sufficient to distinguish those causes.

## Delta classification

### Exact-source closed / proof wired / execution pending

- P0 quota refresh-token ownership;
- P0 combined OAuth/API-Key credential ownership and stale-refresh-token prevention.

### P1 source-reconciled / proof wired / execution pending

- launch confirmation-before-mutation;
- session/provider migration rollback, selected-scope, stopped-target, and provider-bound filtering safety.

### P1 explicit decision still open

- device-code OAuth adoption: implement required parity or explicitly accept a documented capability gap through the decision authority. HMS must not infer acceptance from a baseline constant change.

### P2 non-baseline-blocking workstream

- API Service Live/Realtime breadth;
- WebRTC/sideband and Realtime WebSocket breadth;
- client-secret/session operations;
- larger session-history/catalog UI breadth beyond the reconciled safety invariants.

## Promotion and merge boundary

Current HMS evidence remains frozen at v1.3.28 for the existing sealed epoch only. v1.3.28 is stale for new promotion decisions because live upstream is v1.3.29, but v1.3.29 is not yet adopted.

Do not merge PR #7 unless a Windows Promotion Safety run on the **exact final PR head** materializes Checkout/Setup/Test and passes the complete graph.

A future source-only lane PASS can supply executable HMS source-proof evidence but cannot substitute for the Windows merge gate. Real Windows/current-Codex seven-case execution, a new frozen baseline/review epoch, and a new signed review decision remain separate requirements before production promotion.

## Next actions

1. Materialize an exact-final-head GitHub Actions run where Checkout/Setup/Test actually execute; resolve repository/account runner infrastructure outside the proof code if necessary.
2. Require P0, device characterization/decision contract, launch proof, migration proof, and baseline reconciliation contract to PASS inside the materialized graph.
3. Make the explicit device-auth product decision through `_runtime/HMS_Codex_CockpitV1329DeviceAuthAdoptionDecision.json`; do not infer it.
4. Only after explicit baseline adoption authority may a new v1.3.29 evidence epoch be frozen.
5. Run real Windows/current-Codex seven-case evidence capture and a new signed review epoch before any production promotion.
