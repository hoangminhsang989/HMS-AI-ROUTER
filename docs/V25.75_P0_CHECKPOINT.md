# HMS-AI-ROUTER — v25.75 P0 Checkpoint

Date: 2026-08-23

## Scope completed

- Real Windows/Codex review packet ingest gate.
- Cockpit Tools frozen parity authority is `1.3.28` for v25.75.
- Exact package ZIP / release-manifest SHA-256 checks.
- Signer + trust snapshot consistency checks.
- Freshness and nonce/run/report replay guards.
- Exact 7-case runtime matrix gate.
- Raw evidence is referenced by digest and never rewritten by the workbench.
- Append-only hash-chained `APPROVE` / `REJECT` / `INVALIDATE` decision ledger.
- Two independent pseudonymous reviewers required for each mandatory lane.
- Mandatory lanes: `TERMINAL_PTY`, `PROJECT_RESUME`; `OPTIONAL_GPU` remains optional unless requested.
- Baseline drift freezes eligibility and requires a new review epoch after invalidation.
- `INVALIDATE` may record the newly observed upstream baseline while `APPROVE` / `REJECT` remain pinned to the v25.75 frozen authority baseline.
- Metadata-only workbench export; sensitive token/credential/identity keys are excluded.
- Production Evidence Promotion Auditor is proposal-only authority.
- Claim-boundary and GUI-contract validators added.
- Promotion Workbench Controller persists verified metadata/replay registry/append-only ledger only; no raw packet copy and no raw reviewer identity persistence.
- Reviewer-facing controller action performs a live-baseline callback immediately before ledger publication. Drifted substantive decisions fail-closed to `INVALIDATE`, record `BASELINE_DRIFT_LIVE_RECHECK`, and never authorize automatic production certification or score mutation.
- Principal launcher enters through `_runtime/HMS_GUI_ENTRY.pyw`, extending the large legacy GUI without replacing `_runtime/HMS_GUI.pyw` wholesale.
- Visible product branding in the guarded entry is `AI ROUTER`; legacy compatibility IDs and Cockpit Tools parity/evidence references are not rewritten.
- Guarded entry publishes GUI version `25.75`; launcher falls back to the legacy GUI only if the guarded entry file is physically unavailable.
- Promotion Review is present in the principal GUI.
- Trusted live-baseline provider added for official upstream `jlcodes99/cockpit-tools`, reading GitHub Releases `/releases/latest` over HTTPS with repository/provenance URL, draft/prerelease, response type/size, redirect and semver validation.
- Promotion Review checks the trusted provider asynchronously and displays `MATCH`, `DRIFT`, or `ERROR`; network/provenance failure is fail-closed.
- Reviewer action controls remain intentionally locked until the evidence/reviewer/lane form is fully bound to the controller gate.
- Synthetic E2E negative-path fixtures now cover quarantine, signer/trust failure, stale evidence, replay, baseline drift, single reviewer, reject, invalidate and optional GPU requirements, plus positive two-reviewer/two-lane control.
- Windows GitHub Actions gate compiles all promotion modules + guarded GUI entry and runs provider, ledger, workbench, controller, E2E and GUI-entry proofs.

## Source commits added in this continuation

- `ec8fc16971de1bff3a08f64d2ffe0b2bc2a3eea6` — reviewer action live-baseline recheck gate.
- `d337763dc2a2ac5e465f3d0c7e5fe36c60e7544e` — initial Windows promotion-safety CI gate.
- `91234e69d099c1df0958dfd4dfd4d3c38f3caf5e` — guarded principal GUI entry + Promotion Review page + visible HMS-AI-ROUTER branding.
- `f7d77879b22ecb6fe46a3689870830010c7abbf2` — launcher switched to guarded GUI entry with physical-missing fallback.
- `1ea1e71f47d8456341e580ec45df57eb0627c579` — CI expanded to compile/prove guarded GUI entry.
- `7c686316834488c4268778112b11b6ad4df57a35` — checkpoint for guarded GUI/live-baseline gate.
- `3b50e2095577201d1853934201004fb7cc398b41` — trusted Cockpit live-baseline provider.
- `064decf49f882e0a87c6407f3fba41e6193cb5b9` — provider proof added to Windows CI.
- `849964e7ff1a81005f26c69a8f9238608a0cd492` — GUI asynchronous live baseline `MATCH/DRIFT/ERROR` status.
- `7e0013d8828372e5354268b2c64eddba469b94b7` — promotion E2E negative-path fixtures.
- `6af314f9b5ee5cb9f38dc4698a58ee9f6956d008` — E2E fixtures added to Windows CI.

## Verification status

Previously verified v25.75 source-level/synthetic proofs remain recorded as:

- Python compile: 7/7 v25.75 modules PASS.
- Ingest proof: 6/6 PASS.
- Decision ledger proof after drift remediation: 7/7 PASS.
- Review workbench proof: 5/5 PASS.
- Promotion auditor proof: 6/6 PASS.
- Claim-boundary validator: 6/6 PASS.
- GUI contract validator: 7/7 PASS.
- Workbench controller proof before live-baseline extension: 7/7 PASS.

New source after that verified checkpoint defines:

- Controller proof source: 10 checks.
- Trusted baseline provider proof source: 7 checks.
- Guarded GUI-entry proof source: 6 checks.
- Promotion E2E fixture source: 14 checks.
- Windows CI invokes all of the above plus the existing ledger/workbench proofs.

As of this checkpoint update, the GitHub connector returns no completed status/check context for commit `6af314f9b5ee5cb9f38dc4698a58ee9f6956d008`. Therefore these newly added proof suites are **execution pending/unobserved** and are not claimed PASS here.

All source-level/synthetic safety proofs are **not** Windows production certification and do not count as real external Windows evidence.

## Claim boundary remains frozen

Until a real external Windows + Codex packet is imported and the human review requirements are satisfied:

- `windows_runtime_certified = false`
- `external_windows_target_evidence_imported = false`
- `production_score_promotion_eligible = false`
- feature evidence = `93.0%`
- production evidence = `55.2%`

The v25.75 infrastructure is forbidden from automatically changing production score, automatically certifying Windows runtime, automatically merging upstream changes, or automatically rearming real effects.

## Next P0/P1 work

1. Bind Vietnamese Evidence / Signature / Trust / Freshness / Idempotency / Reviewer A+B / Baseline status to the Promotion Review GUI.
2. Add explicit reviewer identity/salt + lane form and open `APPROVE` / `REJECT` / `INVALIDATE` only when verified packet and live baseline gates permit it; every click must call `record_review_action()` with a fresh provider call.
3. Add GUI-level fixtures for disabled/enabled reviewer controls and live baseline drift between page-open and click-time.
4. Observe the Windows CI result; fix any compile/proof failure before advancing authority.
5. Keep real Windows evidence and final certification blocked until actual target evidence exists.
