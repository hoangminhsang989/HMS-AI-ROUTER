# HMS-AI-ROUTER — v25.75 P0 Checkpoint

Date: 2026-08-23

## Current authority

- Product: `HMS-AI-ROUTER`, Codex-only.
- Frozen Cockpit Tools parity authority for v25.75: `1.3.28`.
- Feature evidence: `93.0%`.
- Production evidence: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.
- No v25.75 module may automatically certify Windows, mutate production score, merge upstream, or rearm real effects.

## P0 completed in source

- Real external Windows/Codex review-packet ingest with exact package/manifest SHA-256, signer/trust, freshness, replay and exact 7-case runtime-matrix gates.
- Append-only hash-chained `APPROVE` / `REJECT` / `INVALIDATE` ledger with two independent pseudonymous reviewers for `TERMINAL_PTY` and `PROJECT_RESUME`; `OPTIONAL_GPU` remains optional unless required.
- Baseline drift freezes the current review epoch. Reviewer-facing publication always performs a fresh live-baseline callback immediately before ledger append; drifted `APPROVE`/`REJECT` fails closed to `INVALIDATE`.
- Metadata-only workbench/controller state; raw packet and raw reviewer identity are not copied into promotion state.
- Trusted live-baseline provider reads official upstream `jlcodes99/cockpit-tools` GitHub Releases `/releases/latest` over HTTPS and validates repository provenance, stable-release status, semver, redirect, content type and response bounds.
- Principal GUI branding displays `AI ROUTER`; legacy compatibility IDs and historical Cockpit parity/evidence strings are preserved.
- Promotion Review GUI exposes Vietnamese gates for Evidence / Signature / Trust / Freshness / Idempotency / Reviewer A+B / Baseline / Ledger.
- Reviewer form contains ephemeral identity, salt, lane and note. Raw identity/salt persistence is forbidden.
- Deterministic Reviewer Action Policy is the safety authority for environment/evidence permissions.
- Promotion GUI Action Contract combines the policy with form validity. `MATCH` can allow `APPROVE` / `REJECT` / `INVALIDATE`; `DRIFT` can allow only `INVALIDATE`; provider error, invalid evidence, busy state or invalid salt blocks all actions.
- Reviewer-policy GUI wrapper adds an explicit confirmation dialog before ledger publication and clears reviewer salt after every action attempt, including cancellation/validation failure.
- Principal launcher path is `_runtime/HMS_GUI_REVIEW_ENTRY.pyw` → `_runtime/HMS_GUI_ENTRY.pyw` → `_runtime/HMS_GUI.pyw` fallback.
- Launcher-chain proof source now verifies the reviewer wrapper remains the principal path and legacy GUI is only the final missing-file fallback.
- Click-time race proof source models page-open at baseline `1.3.28` followed by upstream drift before click; the controller performs a fresh provider call and records only `INVALIDATE` at the newly observed baseline.
- Promotion ledger append is serialized with an exclusive fail-closed lock covering tail re-read, validation and write. Concurrent stale-tail writers cannot both append; the loser must rebuild against the new tail.
- Ledger lock contention has a bounded timeout and is never auto-stolen. A crash-stale lock requires operator inspection/removal rather than unsafe ownership guessing.
- Ledger concurrency proof source checks one stale writer wins, chain integrity is preserved, rebuild against the new tail succeeds, and the lock is released after normal completion.
- Synthetic E2E negative fixtures cover quarantine, signer/trust failure, stale evidence, replay, baseline drift, single reviewer, reject, invalidate, optional GPU and positive two-reviewer/two-lane control.
- Windows CI compiles promotion modules/GUI entries and invokes provider, reviewer-policy, GUI-action-contract, ledger concurrency, launcher/click-race, workbench, controller, E2E, guarded-entry and reviewer-wrapper proofs.

## Latest continuation commits

- `3be511eb53729eb7017f4603d5b1463bd3e7e20c` — Promotion GUI Action Contract.
- `0e7b7f264cd402bc3a5b661f655ec2d51058ba45` — reviewer-policy GUI wrapper with confirmation and mandatory salt clearing.
- `c51373e0289dcf6c8c22d56fa0397df431917dab` — launcher routed through reviewer-policy wrapper with two-level fallback.
- `905dca49a9402fc654775d78397f29150b9ff731` — Windows CI expanded for GUI contract and reviewer wrapper.
- `409df027bccbae325abb75681f7e81752339f432` — ledger append concurrency serialization and stale-tail fail-closed proof.
- `91f2ee0f63fc54271014ea1f328f81a757a52acf` — initial launcher-chain/click-time race proof.
- `44fde56e9d02f42f7622394eb72fda6ea984fa0e` — corrected launcher-chain verifier assumptions.
- `ce7e2b54ebdad98b8e103a3bf854a5f577a7cfa1` — Windows CI expanded for ledger concurrency and launcher/click-time race proof.

## Verification boundary

Previously recorded source-level/synthetic proofs remain valid only for the exact modules/revisions on which they were executed. The newly added concurrency and launcher/race proof entry points are wired into Windows CI.

As of this checkpoint, the GitHub connector returns no completed status/check context for commit `ce7e2b54ebdad98b8e103a3bf854a5f577a7cfa1`. Therefore the newest CI/proof set is **execution pending/unobserved** and is not claimed PASS.

Source-level/synthetic proofs are not real Windows production certification and do not count as external Windows/Codex evidence.

## Next P0/P1 work

1. Prepare the real Windows + current Codex evidence runner/package for the exact seven required runtime cases without weakening existing fail-closed gates.
2. Bind evidence output to package ZIP + release manifest digests and the trusted signer/trust snapshot format expected by the ingest gate.
3. Add an operator-facing Vietnamese preflight that reports missing Codex/Windows prerequisites without creating synthetic evidence.
4. Observe the Windows CI result and remediate any compile/proof failure before advancing authority.
5. Keep production score and Windows certification frozen until actual external target evidence is imported and dual human review completes.
