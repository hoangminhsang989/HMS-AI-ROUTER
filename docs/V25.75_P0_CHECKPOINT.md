# HMS-AI-ROUTER — v25.75 P0 Checkpoint

Date: 2026-08-23

## Current authority

- Product: `HMS-AI-ROUTER`, Codex-only.
- Frozen Cockpit Tools parity authority for v25.75: `1.3.28`.
- Feature evidence: `93.0%`.
- Production evidence: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.
- No v25.75 module may automatically certify Windows, mutate production score, merge upstream, or rearm real effects.

## P0 completed in source

- Canonical real Windows/current-Codex runtime case contract is now explicit and authoritative:
  `host`, `codex`, `quota`, `failover`, `lan`, `soak_6h`, `soak_24h`.
- The exact seven-case set is inherited from the v25.53 Target-Machine Certification runner (`SAFE_STAGES`) rather than invented for v25.75.
- External Windows review-packet ingest now rejects arbitrary seven-ID packets, missing required IDs, unexpected IDs and duplicate IDs.
- Ingest proof explicitly verifies that `case-0` … `case-6` cannot satisfy the runtime matrix.
- Real external Windows/Codex review-packet ingest retains package/manifest SHA-256, signer/trust, freshness and replay gates.
- New fail-closed external Windows evidence runner binds output to package ZIP + release manifest digests, requires the exact seven real case reports, and consumes externally supplied signer/trust JSON.
- Evidence runner preflight requires Windows and a working current Codex CLI, reports blockers in Vietnamese, and never fabricates synthetic evidence.
- The evidence runner performs a local ingest precheck before writing a review packet, but still keeps `windows_runtime_certified=false`, `external_windows_target_evidence_imported=false` and `production_score_promotion_eligible=false`.
- Append-only hash-chained `APPROVE` / `REJECT` / `INVALIDATE` ledger requires two independent pseudonymous reviewers for `TERMINAL_PTY` and `PROJECT_RESUME`; `OPTIONAL_GPU` remains optional unless required.
- Baseline drift freezes the current review epoch. Reviewer-facing publication performs a fresh live-baseline callback immediately before ledger append; drifted `APPROVE`/`REJECT` fails closed to `INVALIDATE`.
- Trusted live-baseline provider reads official upstream `jlcodes99/cockpit-tools` GitHub Releases `/releases/latest` over HTTPS and validates repository provenance, stable-release status, semver, redirect, content type and response bounds.
- Promotion Review GUI exposes Vietnamese gates for Evidence / Signature / Trust / Freshness / Idempotency / Reviewer A+B / Baseline / Ledger.
- Principal launcher path is `_runtime/HMS_GUI_REVIEW_ENTRY.pyw` → `_runtime/HMS_GUI_ENTRY.pyw` → `_runtime/HMS_GUI.pyw` fallback.
- Promotion ledger append is serialized with an exclusive fail-closed lock covering tail re-read, validation and write.
- Windows CI now compiles the canonical case contract, ingest, external evidence runner and existing promotion modules; it executes contract + exact-matrix proofs in addition to prior safety proofs.

## Latest continuation commits

- `c6f51f379d6995d14fbf0407e7c1d34a5d10a62a` — canonical seven-case Windows runtime contract.
- `0a11b8039dc401db458bcf1f60e75742a77a9867` — exact seven-case ingest enforcement and arbitrary-ID rejection proof.
- `dfffd0d9fcccee7d7c8e6b0661dfffe730bc0c5a` — CI contract + ingest proof gates.
- `1c6aad5a3a5e0c54f680f91e789c00f40adae104` — external Windows/current-Codex evidence runner.
- `0b9cb916034665bd64e580c84cbe7792023e029f` — Vietnamese fail-closed preflight diagnostics.
- `e42b30976671158440a9497f73e2a291ae62c385` — CI compile coverage for the external evidence runner.

## Verification boundary

The source changes were syntax-checked before publication and include deterministic proof entry points. GitHub currently returns no completed workflow-run/status context for the latest commit, so the newest Windows CI remains **execution pending/unobserved** and is not claimed PASS.

Source-level/synthetic proofs are not real Windows production certification and do not count as external Windows/Codex evidence.

## Next P0/P1 work

1. Add a Windows operator runbook/example command that invokes the evidence runner with the exact seven report files and trusted signer/trust inputs.
2. Define/verify the external signer + trust snapshot handoff format and make the runner reject malformed signer/trust JSON before packet construction.
3. Add static/CI negative fixtures for missing signer, stale trust, case-content mismatch and non-Windows preflight.
4. Observe the Windows CI result and remediate any compile/proof failure before advancing authority.
5. Keep production score and Windows certification frozen until actual external target evidence is imported and dual human review completes.
