# HMS-AI-ROUTER v25.75 Self-Hosted Windows Runner Fallback

Status: `STAGED_NOT_EXECUTED` on branch `v2575-self-hosted-windows-runner-fallback`.

This branch is deliberately outside PR #7. It cannot certify Windows, adopt Cockpit v1.3.29, import target evidence, change Feature/Production scores, authorize promotion, or authorize merge.

## Current reviewed target

- PR #7 target commit: `53729657756b0b51da550c90461f755ba2d3b005`
- canonical Windows workflow blob: `90eaaa008c4bc9ac93e3b51a2d4e8574c51695d3`
- hosted-runner blocker: `CROSS_REPOSITORY_ACCOUNT_SCOPE_HOSTED_RUNNER_ALLOCATION_FAILURE`
- frozen evidence epoch: Cockpit Tools v1.3.28
- device-auth decision: `OPEN`

If the PR head or canonical Windows workflow blob changes, Stages 4–6 below must be rebound before use.

## Privileged runner boundary

The self-hosted Windows runner is privileged project infrastructure. Keep it offline outside one frozen qualification window. Do not persist registration tokens in repository files, workflows, docs, environment files or shell profiles. Do not use persistent Windows service mode.

Bootstrap authority remains pinned to:

- repository: `https://github.com/hoangminhsang989/HMS-AI-ROUTER`
- install root: `C:\ProgramData\HMS-AI-ROUTER\GitHubRunner`
- custom label: `hms-ai-router-windows`
- `actions/runner` version `2.336.0`
- asset `actions-runner-win-x64-2.336.0.zip`
- exact size `103253740`
- SHA-256 `d59123a43003e357b0805b5d0f611d0bd2f65ab67d51bd070dd4e7a0f685c162`
- automatic runner updates disabled
- foreground-only execution

Setup rejects reparse/junction redirects and non-empty prior runner roots. Registration token input is process environment variable `HMS_GITHUB_RUNNER_TOKEN` and is removed before runner configuration.

## Exact-head workflow authority

The current canonical Windows workflow is structurally prepared for exact-head execution:

- `actions/checkout` pinned to `11d5960a326750d5838078e36cf38b85af677262`
- checkout ref `${{ github.event.pull_request.head.sha || github.sha }}`
- persisted Git credentials disabled
- immediate `git rev-parse HEAD` equality check
- `actions/setup-python` pinned to `a26af69be951a213d495a4c3e4e4022e16d87065`
- X.509 verifier package pinned to `cryptography==50.0.0` with runtime version assertion

The P0/P2 source workflows use the same exact-head checkout/verification and immutable action pins. None of these guards has executable PASS authority until a runner actually materializes the steps.

## Recovery ladder

Do not skip stages.

### Stage 0 — restore normal hosted allocation first

Check account Billing / Actions usage / included minutes / spending limits / payment state and repository **Settings → Actions → General**. Do not record a billing/quota/payment root cause without direct owner-side evidence.

If hosted allocation recovers, keep the canonical workflow hosted and do not activate this fallback.

### Stage 1 — register foreground runner

Use a fresh repository-level runner token immediately before registration:

```powershell
$env:HMS_GITHUB_RUNNER_TOKEN = '<fresh repository runner registration token>'
.\scripts\setup_self_hosted_windows_runner.ps1
```

Confirm repository Settings shows the expected runner and label.

### Stage 2 — local preflight

```powershell
.\scripts\start_self_hosted_windows_runner.ps1 -CheckOnly
```

Required properties: ready to start, foreground mode, no service mode.

### Stage 3 — connectivity canary

Start the runner in foreground and manually dispatch:

`v25.75 Self-Hosted Windows Runner Canary (Fallback Only)`

Only actual execution of the `Runner materialized` step counts. Authority remains `CONNECTIVITY_ONLY_NOT_WINDOWS_PROOF`.

### Stage 4 — exact-target binding

Manually dispatch:

`v25.75 Self-Hosted Windows Target Binding (Fallback Only)`

Defaults are the reviewed target commit/blob above. The workflow pins `actions/checkout` and separately checks out exact target bytes and exact fallback-tooling bytes. The helper fails closed on wrong commit, wrong workflow blob, wrong repository identity, or any tracked/staged/untracked residue.

Authority remains `TARGET_BINDING_ONLY_NOT_WINDOWS_PROOF`.

### Stage 5 — activation-candidate audit

Manually dispatch:

`v25.75 Self-Hosted Windows Activation Candidate Audit (Fallback Only)`

It may generate only this exact one-line change:

```yaml
runs-on: windows-latest
```

to:

```yaml
runs-on: [self-hosted, Windows, X64, hms-ai-router-windows]
```

Reverse transformation and independent `git diff --no-index` must prove zero other payload changes. The candidate is never committed or executed and is deleted after audit.

Authority remains `ACTIVATION_PATCH_ONLY_NOT_WINDOWS_PROOF`.

### Stage 6 — canonical graph activation

Only after Stages 3–5 have real step/log PASS evidence may a separate routing change be considered. The complete canonical Windows Promotion Safety graph must remain intact and must execute on the exact final PR head.

## Merge/evidence boundary

None of these is sufficient to merge PR #7: fallback branch existence, runner registration, Online/Idle state, connectivity PASS, target-binding PASS, activation-candidate PASS, source-only PASS, or static exact-head hardening.

Before merge, Windows Promotion Safety must actually materialize its exact-head checkout/verification and pass the complete graph. Real Windows/current-Codex seven-case execution, a new signed evidence/review epoch, and human production promotion remain separate later gates.

After a qualification window, stop the foreground runner, remove its repository registration, confirm it is offline, preserve required diagnostics, and only then remove the dedicated local runner directory after confirming ownership.
