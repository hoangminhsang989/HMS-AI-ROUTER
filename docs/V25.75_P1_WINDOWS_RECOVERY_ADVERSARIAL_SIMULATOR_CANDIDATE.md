# HMS-AI-ROUTER — v25.75 P1 Windows Recovery Adversarial Simulator Candidate

Date: 2026-08-24
Status: CANDIDATE — CI verdict pending

## Purpose

Extend source/synthetic recovery assurance without pretending to replace the authorized real-Windows target gate.

`_runtime/HMS_Codex_WindowsRecoveryAdversarialSimulator.py` deterministically exercises adversarial states against the existing recovery contracts and the pure official-auth client-lifecycle derivation.

Coverage includes:

- UAC eligibility and one-shot replay boundaries;
- background-vs-interactive recovery behavior;
- PID substitution from supported client to unrelated processes;
- PID disappearance between discovery and revalidation;
- mixed surviving/exited targets;
- duplicate, invalid, current-process and 32/33 PID bounds;
- canonical numeric-only taskkill argument construction;
- structured official-auth policy states: unknown, not-requested, restart-disabled, replacement PID success, old+new PID collision, consumed UAC epoch;
- recovery classification and sensitive-detail redaction;
- explicit proof that the simulator itself never calls the real elevation helper, real process enumeration, Win32 UAC runner or production authority.

The existing `HMS_Codex_WindowsUACRecoveryValidation.py proof` imports this simulator only inside proof mode. `preflight` and `interactive` target modes do not import or execute the simulator.

## CI observation state

PR #4 head before this checkpoint produced Windows Actions run #69, but both attempt 1 and attempt 2 failed before any job step was materialized. No Checkout/Setup/Test step or job log was available, so this is not classified as a simulator assertion failure.

A fresh head commit is intentionally used to request a new independent Windows run.

## Evidence boundary

Unchanged:

- Feature evidence: `93.0%`.
- Production evidence: `55.2%`.
- `windows_runtime_certified = false`.
- `external_windows_target_evidence_imported = false`.
- `production_score_promotion_eligible = false`.

This checkpoint MUST NOT be promoted to PASS until a Windows workflow run executes the proof graph and completes successfully.
