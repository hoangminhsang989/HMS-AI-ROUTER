# HMS-AI-ROUTER — v25.75 Windows CI Observation Probe

Date: 2026-08-24

Purpose: trigger the existing proof-only `v25.75 Windows Promotion Safety` workflow through a pull request so its Windows run can be observed through the connected GitHub tooling.

This probe intentionally changes no runtime logic and authorizes no UAC-interactive execution, Windows certification, external target-evidence import, or production-score mutation.

A successful CI run proves only that the configured Windows proof graph passed on the GitHub-hosted Windows runner. It does not replace bounded real target-machine UAC validation and does not replace the canonical seven-case Windows/current-Codex production certification gate.
